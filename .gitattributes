# Normalize line endings; force LF for files that run inside Linux containers
# so a Windows checkout can't introduce CRLF that breaks bash/Docker.
* text=auto eol=lf
*.sh text eol=lf
Dockerfile text eol=lf
*.png binary
*.jpg binary
*.ico binary
